pipeline {
    agent none

    stages {
        stage('Build') {
			agent 
			options { 
				skipDefaultCheckout() 
			}

            steps {                
                echo 'Hello World'
            }
        }
    }
}
